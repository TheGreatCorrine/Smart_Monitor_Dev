# Smart Monitor and Alert System Dependencies
# Core dependencies for the project

# YAML configuration parsing
PyYAML>=6.0,<7.0

# Testing framework
pytest>=7.0,<9.0

# Web framework (for web interface)
Flask>=3.0,<4.0

# Data processing and utilities
numpy>=1.21,<2.0

# Development and testing tools
pytest-cov>=4.0,<5.0
black>=23.0,<24.0
flake8>=6.0,<7.0

# Optional: GUI dependencies (only if using GUI)
# tkinter is built-in with Python, no need to install separately 